# 优化方案二次完整性复核与 Wave 0 实施报告

> 复核日期：2026-07-18
> 复核对象：22 份现行教材、203 个已识别章节、110 项优化任务、实验/项目/reference 与发布治理资产
> 证据边界：作者侧内容、代码、状态和本地门禁；不包含目标学习者试学或生产环境认证

## 一、最终判断

本轮已经完成 Wave 0 的 10 项 P0 阻断任务，并修复二次反向检查发现的 E06-01 客户端权限
输入残留。八套 Python 3.13 reference 共 149 项测试通过，逐文件清单、任务矩阵、来源许可、
RQ01 发布证据、版本、仓库结构、结构化数据、编码、内容 blocker、Mermaid、PowerShell 语法、
Compose、Kustomize、Python 编译和 Git 发布候选差异门禁均已接入统一入口。

当前仓库可以公开为**持续建设的结构化教材与 reference 学习库**，但不能表述为“全书完成”、
“全部章节自学就绪”“生产平台完成”或“开源教材”：

- 110 项矩阵仍有 10 项 `in-progress`、2 项 `blocked`、87 项 `planned`。
- 更广的 14 项 P0 中，10 项完成，4 项仍在进行。
- 真实 vLLM/Triton、真实 P03 服务回放/trace、RQ01 clean final 和全库答案隔离未完成。
- 没有目标学习者的整体试学、迁移题或保持度证据。
- 仓库没有根级许可证；公开可见不等于授予复制、修改或再分发许可。

## 二、二次反向检查

| 检查项 | 结果 | 判断 |
|---|---:|---|
| 过期 P03 `v0.3.0` | 0 命中 | 现行版本统一为 v0.3.1 |
| P03 `BLPOP/BRPOP/LPOP/RPOP` | 0 命中 | 实现已迁移为 Redis Streams consumer group |
| Python 3.8/3.9 作为现行验收基线 | 0 个未说明命中 | 旧版本只保留为 EOL/历史/禁止验收说明 |
| 八套 reference 的 Python 范围 | 8/8 为 `>=3.13,<3.14` | 与主学习基线一致 |
| 客户端自报 tenant/user/permission | 修复后 0 个现行契约残留 | E06-01 补 server-owned Principal、ACL snapshot 与负向验收 |
| 设计稿冒充真实 serving | 0 个已知未说明入口 | vLLM 保持 blocked，Triton 保持 design-only |

E07-03 仍会出现一次“关闭旧 `BLPOP` 窗口”的历史安全说明；它不是当前实现调用，不能据此
判断 Redis List 契约复发。

## 三、Wave 0 验收

| 任务 | 已落实内容 | 主要证据 |
|---|---|---|
| `W0-001` | 区分单次 fixture、30-seed pilot 与可外推科研结论 | P01 实验记录、RQ01 记录 |
| `W0-002` | 金融路线拆分结构、内容、reference、教学与学习者状态 | 金融路线、模块状态表 |
| `W0-003` | 看板和总控采用多维状态，不再自动升格 | 状态表、整改总控 |
| `W0-004` | server-owned Principal、检索前 ACL、伪造/跨租户负测 | E03/P03 security tests |
| `W0-005` | Agent 身份、授权、审批 CAS、审计、恢复与 fencing 统一 | E04 runtime，38 tests |
| `W0-006` | SQLite owner/version/lease finalize fencing | E06 database 与 tests |
| `W0-007` | Redis Streams pending reclaim，数据库终态后 ACK | P03 queue/worker tests |
| `W0-008` | M06/E06/P03 统一 outbox、错误、lease 与缓存维度 | 教材、E06/P03 tests |
| `W0-009` | 合成 simulator、真实 vLLM 与 Triton 证据分层 | M10/E10 入口 |
| `W0-010` | 20 项路线承诺映射到首次教学位置与缺口动作 | 路线承诺覆盖矩阵 |

详细逐项原因与文件路径见
[[00_路线总控/审计与整改/artifacts/governance/optimization_matrix_status_evidence.csv|状态证据表]]。

## 四、矩阵真实状态

| 状态 | 数量 | 含义 |
|---|---:|---|
| `completed` | 11 | 10 项 Wave 0 加矩阵治理任务 `PLAN-001` |
| `in-progress` | 10 | 已有直接产物，但前置或完整验收未闭环 |
| `blocked` | 2 | 真实 vLLM 与 NVIDIA Triton 缺执行环境和证据 |
| `planned` | 87 | 尚未形成任务动作对应的新闭环产物 |

更广 P0 中仍进行的 4 项是 `M02-002`、`M03-004`、`M04-002`、`M06-003`。它们已有部分实现，
但仍受请求生命周期/并发 API、ingestion 与模型边界、egress/path/URL 对抗、数据库/分布式
失效语义等前置约束，不能因下游测试通过而提前标记完成。

矩阵 validator 已验证 110 行、17 列、110 个唯一任务、172 条依赖、0 个循环、0 个错误；
14 项描述型外部前置单独报告，不误判为悬空任务 ID。

逐文件覆盖清单与任务矩阵承担不同职责。前者已按发布候选重建为 461 行、15 列，覆盖
461/461 个纳入治理的教材与可执行文件：320 行为 `rule-reviewed`，141 行为 `unreviewed`；
243 行为 `content-reviewed`，77 行为 `draft`，141 行保持 `unassessed`。逐文件 validator
检查表头、路径唯一性、仓库全覆盖、状态枚举和组合约束，并禁止给未复核文件自动推断成熟度。
因此“已覆盖”只表示进入清单，不表示“已人工审完”或“已达到自学就绪”。

## 五、Reference 验证

| Reference | 测试数 | 覆盖重点 |
|---|---:|---|
| P01 Mini Scheduler | 28 | 调度、尾延迟、aging、科研 pilot |
| E02 Task API | 12 | API 契约、Principal、owner 隔离、OpenAPI |
| E03 RAG | 21 | ACL 预过滤、评估、可信 metadata、威胁边界 |
| E04 Agent Runtime | 38 | schema、授权、审批、恢复、fencing、session、脱敏 |
| E06 SQLite | 7 | task/outbox、lease、stale-worker fencing |
| E10 Inference | 7 | 确定性模拟与延迟统计 |
| Finance Reference | 9 | 跨资产收益率、未来标签、久期与凸性 |
| P03 Workload Platform | 27 | FastAPI/PostgreSQL/Redis Streams、RAG、worker 恢复 |
| **合计** | **149** | 只证明 reference，不代表学习者完成或生产可用 |

E09 的 kind/Kubernetes 功能性 reference 依赖 Docker/本地集群，未混入纯 Python 计数。

## 六、统一门禁

发布候选使用以下入口复跑：

```powershell
.\00_路线总控\审计与整改\tools\bootstrap_reference_envs.ps1
.\00_路线总控\审计与整改\tools\run_full_validation.ps1
```

最终结果为 `full_validation=passed`，机器证据位于
`00_路线总控/审计与整改/artifacts/full_validation_2026-07-18/`。统一入口覆盖：

- 八套 pytest 149 项；
- 逐文件覆盖清单、优化矩阵、来源/许可台账、RQ01 发布证据和版本 manifest 五个治理 validator；
- Markdown 围栏、Python/JSON 块、正式 WikiLink 与占位 WikiLink 分流；
- 路径/文本严格 UTF-8、NFC、控制字符与名称冲突；
- 内容质量工具链，最终 blocker 为 0，advisory 不折算为质量分数；
- Mermaid CLI 真实渲染，失败为 0；
- 八套 reference 的逐项目精确测试数、总计 149、failure/error/skip 均为 0；
- 13 个 PowerShell 脚本语法、`docker compose config` 和 `kubectl kustomize`；
- 内容工具生成证据后解析 98 个 CSV、83 个 JSON 和 8 个 TOML 文件；
- Python 3.13 `compileall`，以及工作树、真实暂存区和临时发布候选索引的
  `git diff --check`。入口还要求已跟踪工作树与暂存区一致，且不存在未跟踪发布候选。

发布前复核发现，旧门禁只检查已跟踪工作树差异，会漏掉当时尚未跟踪的新文件。现行入口改用
临时 Git index 纳入所有非忽略发布候选，并保留工作树与真实暂存区检查；首轮检出的 90 个文件
行尾问题已规范化。逐文件台账也已拆分为“教材与可执行源文件覆盖及自动分诊清单”和“历史审计快照”：当前
清单中的 `unreviewed` 文件一律保持 `unassessed/unknown`，不再用路径规则伪造 `planned/low`
结论。模块成熟度以模块状态表为准，110 项整改任务及其证据表是任务状态真源。

Mermaid 结果按运行日期写入对应的 `content_quality_audit_YYYY-MM-DD`，不再覆盖
2026-07-11 的历史证据目录。

结构化数据复核发现 26 个 Locust CSV 含 54 个空记录，现已清理。RQ01 发布证据同时修复
`README.md` 哈希漂移、被忽略的 `stdout.log` 清单项和源码 tar 路径前缀不一致；独立
validator 检查 12 个发布文件、压缩包安全路径和 17 个源码成员的 SHA-256。路径计算实现
兼容 Windows PowerShell 5.1，不依赖该环境缺失的 `Path.GetRelativePath`。

门禁复跑还发现 P03 固定回放测试把并发语义写成 `<30ms` 的本机计时阈值。该断言已改为
`slow-start < next-start < slow-finish` 的事件顺序不变量，并连续复跑 30 次通过；这既能检测
串行等待回归，也不会把 Windows 调度抖动误报为功能失败。

`-SkipContentAudit -SkipMermaid` 只用于快速冒烟，不能替代这里的完整发布门禁。

## 七、治理与发布边界

- 来源/许可台账有 33 项：Obsidian Local REST API bundle 的 MIT 许可已核验，另外 32 项仍为
  `review-required`。台账通过只证明结构和证据路径有效，不代表完成法律判断。
- 版本 manifest 有 22 个组件。Pydantic 2.13.4 在 E04 直接锁定，但在 E02/P03 仍是 FastAPI
  的传递依赖，因此仓库级状态继续为 `review-required`。
- 真实 vLLM 保持 `blocked`；NVIDIA Triton Inference Server 与 Triton language/compiler
  保持 `design-only`，直到版本、环境、兼容矩阵和执行证据齐全。
- Git 发布候选约 46 MiB，无超过 GitHub 100 MiB 的文件；31.18 MiB 的
  `per_task_results.csv` 是 RQ01 逐任务研究证据。
- Obsidian `data.json*`、kubeconfig、`.venv`、`.tools`、日志、证书和私钥只允许留在 Git
  忽略边界；不得把整个工作目录压缩后上传。
- 发布历史使用 GitHub `noreply` 邮箱，避免把本机个人邮箱写入公开提交历史。

## 八、下一阶段

1. 先完成 4 项进行中的 P0，并保持 DAG 前置，不以局部测试跳过基础章。
2. 推进 M00-M02 请求生命周期、并发/取消、工程门禁和生产认证边界。
3. 推进 M03-M06 ingestion/eval、egress/path/URL 对抗、真实队列语义和分布式可靠性。
4. 在独立环境完成真实 vLLM/Triton、E09 HPA/KEDA 与生产安全；此前保持 blocked/design-only。
5. 完成 RQ01 clean final、Pareto/burst/真实服务回放与场景 trace，避免从合成 pilot 外推。
6. 逐步补齐 159 个教学章的 worked example、机制型反例、独立练习、反馈、章内来源和答案隔离。
7. 单独开展目标学习者试学、迁移题和保持度验证；它不由作者侧门禁自动获得。

## 九、结论

本轮达成的是“P0 阻断收口、reference 可复现、状态诚实、发布边界可审计”，不是“所有教材
建设任务完成”。因此可以开始按已开放入口自学并公开仓库，但后续进度必须继续以模块状态、
整改任务真源、验收证据和学习者实际记录为准。
