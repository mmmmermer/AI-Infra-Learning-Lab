# 整改变更日志

## 2026-07-18

### 最终发布候选 / 后续加固（现行结论）

> 本节晚于下方同日早期快照；当前数字以本节、现行状态表和机器校验结果为准，下方
> 461/149/203 等数字保留为当时事实，不回写成最终口径。本节记录候选范围与已取得证据，
> 不单独构成本轮完整门禁通过结论；发布结论只以同一轮完整门禁的新摘要为准。

- 新增 E00 进程/网络与 E01 并发 reference，并加固 E02/E03/E04/E06 的生命周期、并发、
  授权、注入、取消、副作用、lease 和重试反例。十套 Python 3.13 reference 最终为 257 项：
  E00 11、E01 6、E02 29、E03 35、E04 76、E06 29、E10 7、P01 28、Finance 9、P03 27。
- E02 独立复核修复 OpenAPI `ProblemDetails` 内联后的悬空 `$ref`，并补整份文档本地 JSON
  Pointer 解析、提交点依赖失效、deadline 无幽灵写、并发幂等恰好一次和跨 owner/tenant 隔离。
- 现行教材范围为 22 份现行加 1 份归档、210 章；内容类型为 166 `instructional`、2
  `workbook`、4 `reference`、27 `design-note`、11 `appendix`，`unclassified=0`。166 个教学章
  中显式 worked example 为 78、显式章内来源为 51；这些结构信号不解释为教学质量得分。
- 110 项优化矩阵经逐项证据复核后为 `completed` 24、`in-progress` 8、`blocked` 2、
  `planned` 76；14 项 P0 为 13 项完成、`M03-004` 进行中。证据表以 34 行覆盖全部非
  `planned` 任务，真实 vLLM/Triton 仍保持 `blocked`。
- 逐文件清单按最终文件范围重建为 494 行并通过 494/494 覆盖校验：353 行
  `rule-reviewed`、141 行 `unreviewed`；274 行 `content-reviewed`、79 行 `draft`、141 行
  `unassessed`。未复核行继续保持 `unknown` 风险，不由生成器自动升降级。
- 版本 manifest 现有 23 个组件，新增 psycopg 3.2.9，并区分 P03 PostgreSQL 17.5 容器基线与
  E06 本机 PostgreSQL 17.9 deadlock 证据；无过期组件。
- 统一门禁扩展为十套 reference，并从逐项目 `ExpectedTests` 动态计算总数；pytest 使用
  `-X dev -W error::ResourceWarning`。E00/E01 虽为 verified reference，学习者复现仍未完成；
  E03 generation、真实 vLLM/Triton、E09 HPA/KEDA、RQ01 final 和整体试学继续保持未完成。

### 同日早期快照

- 公开边界复核已完成：私有 Rubric 与真实工程案例仅保留作者自有抽象，分别登记为
  `EXT-RUBRIC-001`/`EXT-CASE-001`；原件、内部路径、旧项目标识和受控链接不随仓库发布。
- 统一门禁的绿色摘要改为在最终 staged secret scan、diff check 和候选 tree OID 校验全部通过后
  才写入，避免中断过程留下可被误读的完整通过证据。
- 将逐文件清单升级为 15 列治理 schema，并按最终发布候选重建为 461 行：320 行由明确规则
  复核，141 行保持 `unreviewed/unassessed/unknown`。独立 validator 验证 461/461 文件覆盖、
  表头、唯一相对路径、枚举值、状态组合和未复核文件不得被推断成熟度，0 个错误。
- 加固统一发布门禁：入口拒绝未暂存的已跟踪变更和未跟踪发布候选；八套 reference 必须
  精确收集 149 项，且不允许 failure、error 或 skip；新增 RQ01 artifact、结构化数据、
  13 个 PowerShell 脚本语法、Compose 配置和 Kustomize 构建检查，并在内容审计生成证据后
  解析 98 个 CSV、83 个 JSON 和 9 个 TOML 文件。
- 修复 26 个 Locust CSV 中的 54 个空记录；刷新 RQ01 `README.md` SHA-256，移除清单中被
  忽略的 `stdout.log`，统一源码快照 tar 的成员路径。RQ01 validator 现检查 12 个发布文件、
  archive 安全路径和 17 个源码成员的逐文件哈希；相关 PowerShell 路径计算兼容 Windows
  PowerShell 5.1，不再依赖 `Path.GetRelativePath`。
- 对 110 项教材内容优化矩阵逐项复核状态与前置，结果为 `completed` 11、`in-progress` 10、
  `blocked` 2、`planned` 87；新增独立状态证据表和审计报告。矩阵 validator 验证 110 行、
  17 列、172 条依赖、0 个循环和 0 个错误。
- `W0-001` 至 `W0-010` 的 P01/科研措辞、金融状态、状态模型、RAG ACL、Agent 审批、
  SQLite fencing、Redis Streams、跨层契约、推理范围和路线覆盖任务均达到自身验收。
  这 10 项完成不等于全部 14 项 P0 完成；M02-002、M03-004、M04-002、M06-003 仍按
  依赖与剩余验收保持 `in-progress`。
- 新增 E04 确定性 Agent Runtime reference：请求白名单、server-owned Principal、
  capability/resource gate、检索前 ACL、审批 version CAS、不可变审计、resume outbox、
  lease reclaim/fencing、session tenant/owner/CAS 隔离和日志脱敏共 38 tests 通过。
- 反向复核发现并修复 E06-01 的客户端 `permission_group` 残留：任务只接受业务输入，
  tenant/user/ACL snapshot 由可信认证层写入；伪造身份/权限字段返回 422 且不产生 task/outbox，
  非 owner 查询保持隐藏式 404。
- 八套 Python 3.13 reference 最终回归共 149 项：P01 28、E02 12、E03 21、E04 38、
  E06 7、E10 7、Finance 9、P03 27。引导脚本可从 ASCII reference 目录名与 lock 文件
  自动发现 8 个项目，规避 Windows PowerShell 5.1 对无 BOM 中文脚本路径的错误解码。
- 完整门禁捕获 P03 固定回放测试的 `<30ms` Windows 调度抖动；将断言改为
  `slow-start < next-start < slow-finish` 的事件顺序不变量，既能拒绝串行等待，又不依赖
  本机计时阈值。目标用例重复 30 次全部通过。
- 修复 Mermaid validator 将当前结果写入 2026-07-11 历史目录的问题；独立工具和统一门禁
  现在都按运行日期写入 `content_quality_audit_YYYY-MM-DD/mermaid_validation.json`。
- 新增来源/许可台账、版本 manifest、矩阵状态证据及三个 validator，并全部接入统一门禁。
台账 33 项中仅 Obsidian Local REST API bundle 的 MIT 许可已核验，其余 32 项继续
  `review-required`；根级许可证尚未选择，公开可见不表述为开源授权。
- 统一当前入口的 E02/E03/E04/E06/Finance/P03 测试数字和真实成熟度；历史日期报告保留
  当时快照。根 README 明确结构化教材、reference、教学就绪和学习者验证四类独立边界。
- M02 新增“认证 Principal 与资源授权”教学章后，现行范围由 202 章增至 203 章：
  `instructional` 159、`workbook` 2、`reference` 4、`design-note` 27、`appendix` 11。
  同步更新范围断言、验证清单和矩阵缺口基线，保留旧报告中的 202 章历史快照。
- 发布安全复核确认 Git 候选约 46 MiB、无超过 GitHub 100 MiB 的文件；31.18 MiB 的
  `per_task_results.csv` 保留为研究证据。Obsidian `data.json*`、kubeconfig、`.venv`、
  `.tools`、日志、证书和私钥只存在于 Git 忽略边界，禁止按整个工作目录打包发布。
- 完整统一门禁通过：八套 reference、治理 validator、仓库/编码、内容 blocker、Mermaid
  渲染、`compileall` 与 `git diff --check` 均通过；具体机器证据见
  `artifacts/full_validation_2026-07-18/`。剩余真实 vLLM/Triton、真实 P03 服务回放/trace、
  RQ01 clean final、全库答案隔离和学习者试学继续保持未完成边界。

## 2026-07-13

- 完成作者侧知识覆盖、工程强度、科研基础与讲解质量复核；范围为 22 份现行教材、202 章，
  并交叉检查直接依赖的实验、P01、P03、RQ01 和 reference。新增总审计报告，明确使用
  E0-E3、R0-R3、F0-F3 定位能力，不把结构信号或 reference 通过率当成教学质量合格率。
- 新复核确认七类 P0：M03/P03 客户端身份与权限示例、E04 审批契约、E06 stale-owner fencing、
  P03 `BLPOP` 后 claim 的丢消息窗口、M10 vLLM/Triton 发布范围、P01 单次 fixture 的“显著”
  措辞、金融路线完成声明与状态表冲突。本轮只登记和规划，没有把这些问题写成已修复。
- 新增并扩展为 110 行教材内容优化任务矩阵：P0 14、P1 75、P2 20、P3 1；每项记录章节、知识缺口、
  解释缺口、目标工程/科研/金融等级、前置、产物、验收和 canonical source。
- 优化顺序固定为正确性与状态口径、M00-M02 基础链、M03-M06 可靠系统、M07-M10 运行工程、
  M11/F 系列研究基础、全库教学一致性；F08/M12 和未实跑能力继续保持 design-note/blocked。
- 新矩阵经 `Import-Csv` 验证为 110 行、17 列、ID 唯一且无空字段。本地统一门禁补跑通过：
  七套 reference 共 82 项，312 个 Markdown 结构/代码/WikiLink 错误为 0，657 个文件编码问题为 0，
  `compileall` 与 `git diff --check` 通过；30 份含 Mermaid 文档渲染失败为 0。
- 独立内容审计为 `passed_with_advisories`、blocker 0。Lychee 扫描 324 个唯一 URL 时因当前网络
  出现 109 个访问/协议错误结果和 69 个超时结果，未出现 404/410；首次统一入口在完成内容审计、
  Mermaid 和差异检查后达到 20 分钟父进程上限，最终编码由随后本地补跑完成并写入
  `summary_smoke.json`，未把两次运行表述成一次无超时完整通过。

- 完成 Wave 2 第二批教学闭环：M00 第 7 章改为离线 HTTPX `MockTransport` 正负链路；
  M01 第 7 章修正 `scheduler/io.py` 的 `parents[1]` 项目根语义并补 JSON 往返、错误上下文和
  临时目录清理；M02 第 2、4、9 章补资源决策、八类 Pydantic 输入矩阵和创建/查询/metrics
  状态观察。五章均补独立变式、验收、来源和章末衔接。
- E02 在不增加重复测试数量的前提下加强契约：逐请求拒绝 `task_id/status/submit_time/created_at`
  等服务端字段，校验合法且不同的 UUID 和带 UTC offset 的时间；仍为 9 个运行时用例加
  1 个 OpenAPI schema 用例，实跑 `10 passed`。
- 独立复核发现 M02 的 E02/P03 图示混合、fixture teardown 过度声明、UUID/UTC 断言不足、
  来源不就近和片段角色缺失，已逐项修复。首次完整门禁据此捕获 Mermaid 元数据顺序错误；
  M02 单文档 4 张图修复后全数渲染，再顺序重跑完整入口通过。
- 最终结构快照为 1,527 个教材代码围栏、360 个视觉项；158 个教学章的目标、worked example、
  反例、练习、验收、章内来源为 135/61/112/108/121/39；章内 1,415 个围栏中 49 个已标角色，
  34 个 Mermaid 中 6 个具有显式可访问性元数据。自动计数仍只用于复核排序。
- 完整门禁通过：七套 reference 共 82 项，311 个 Markdown 的围栏、Python/JSON 与正式
  WikiLink 错误为 0，30 份含 Mermaid 文档渲染失败为 0，compileall、diff 和前后编码门禁通过。
  内容审计为 `passed_with_advisories` 且 blocker 0：Markdownlint 106、Zhlint 7295、Vale
  34 raw/0 confirmed、Codespell 0、Git private-key 0、jscpd 16 组 Markdown 候选；311 个
  唯一 URL 中 25 个访问/协议异常、0 个确认 404/410。

- 完成 Wave 2 第一批八个高影响教学锚点章改造：F01 第 1 章、F04 第 4 章、F05 第 3-4 章、
  M00 第 8 章、M01 第 12 章和 M02 第 7-8 章。统一补目标与前置、固定输入、中间状态、
  实际输出、错误恢复、独立变式、验收、章内来源、总结和下一步；新增围栏显式标记
  `runnable`、`fragment` 或 `output`。
- M00 Git 章节先写 `.gitignore` 再创建 `.venv`，避免把虚拟环境加入索引，并增加 Git 身份、
  staging 和并列排序验收；M01 增加可独立运行的 `argparse` CLI，把依赖尚不存在
  `scheduler.io` 的代码明确降为 `fragment`，未虚构 E01-04；M02 把每个 422 请求限制为
  单一无效条件，并以可执行 schema 断言验收 OpenAPI。
- E02 删除不在教学范围内的空 `GET /workers` stub，新增 OpenAPI schema 测试；当前为
  9 个运行时 API 契约用例加 1 个 schema 用例，实跑 `10 passed`。该结果属于 reference，
  不写成学习者完成。
- 固定数值复核通过：F01 fixture 的均值为 `0.001`、样本标准差为 `0.0138744369`；F04
  看涨期权基准价为 `9.413403`，升波动率/降波动率/缩短期限分别为 `11.348477`、
  `7.485088`、`6.371028`；F05 两个目标全价 101/98 的 YTM 分别为
  `0.044662591629` 和 `0.0609228474`。
- 158 个教学章的显式目标、worked example、反例、练习、验收和章内来源由
  127/51/107/101/109/26 增至 130/58/108/105/117/35；总结由 12 增至 20，下一步由
  5 增至 13，显式代码角色由 7 增至 21。自动计数只用于复核排序，不解释为质量合格率。
- 接续增强 M00/M01 第 7 章：M00 以 HTTPX `MockTransport` 建立不启动服务、不访问公网的
  HTTP/JSON 正负链路；M01 修正从 `scheduler/io.py` 推导 `project_root` 的层级语义，补文件
  往返、坏 JSON、缺字段、未知字段和临时目录清理。两章新增 18 个围栏均有显式角色。
- 接续增强后，158 个教学章的六类信号进一步变为 135/61/112/108/121/39；总结为 25，
  下一步为 18，显式代码角色为 43。当前 22 份教材共有 1,527 个代码围栏和 360 个视觉项；
  34 个 Mermaid 中显式可访问性信号为 6 个。章节分析与官方格式分析器共 13 项单元测试通过，正式
  `official_docs_structure.json` 和 202 行 `chapter_matrix.csv` 已刷新。
- 按当前文件集合重建逐文件整改矩阵为 427 行；E02 reference 各代码/测试行统一记录为
  9 个运行时契约加 1 个 OpenAPI schema 测试通过，不再沿用旧的 3-test 描述。

## 2026-07-12

- 完成 Wave 1 章级人工归类：202 章现为 158 `instructional`、2 `workbook`、4 `reference`、
  27 `design-note`、11 `appendix`；`unclassified=0`，未知类型和同章冲突均为 0。
- 22 份现行教材入口统一补齐目标读者、先修/环境诊断、范围、版本边界、作者侧预计用时、
  学习产物、完成口径、学习顺序和内容类型；预计用时明确不是学习者实测或发布门禁。
- F04/F05 的候选金融任务、F08/M12 全书、M04 未实现审批/Agent Runtime、M07 简化 Compose、
  M09 迁移路线和 M10 真实 vLLM 继续保持 design-note 或 blocked 边界，不因结构化而升级状态。
- M01/M02 集中验收章归为 `workbook`；M00/M06/M07 来源索引归为 `reference`；导航、
  暂不深入和简历表达等归为 `appendix`，避免稀释教学正文缺口。
- 新的教学正文分母为 158 章：显式目标 127、worked example 51、反例 107、练习 101、
  验收 109、章内来源 26。下一波优先补 worked example、章内来源、反馈/清理和章末衔接；
  自动计数仍不解释为质量得分或学习效果。
- 刷新官方文档格式 JSON/CSV、主报告、证据 README、整改总控和矩阵生成规则；旧的 104 章
  未分类数字只保留在历史变更记录和 Wave 1 原始任务描述中。
- Wave 1 完整门禁通过：七套 reference 共 75 项测试，311 个 Markdown 的围栏、Python/JSON
  与正式 WikiLink 错误为 0，30 个 Mermaid 文档渲染失败为 0，654 个文件编码无问题，
  compileall 和 `git diff --check` 通过；内容审计仍为 `passed_with_advisories` 且无 blocker。

- 完成官方技术学习文档格式与内容专项检查：对标 Microsoft Learn、Google、Kubernetes、
  Python 和 GitHub Docs 的 20 个官方页面；明确直接规范、官方实现惯例和信息架构的证据层级，
  不把时长、单一占位符样式或固定行宽误写成通用门禁。
- 将教材状态拆为 `content-reviewed` 与 `learner-validated`。本轮明确排除试学、本人复现、
  前后测、迁移和延迟保持，只检查作者侧内容、代码、事实、安全、版本、来源和发布结构。
- 完成 22 份现行教材、202 章人工全覆盖复核：金融 F00-F08 共 63 章，工程 M00-M12
  共 139 章；新增逐章 CSV/JSON 证据和独立主报告，不使用自动分数计算“规范符合率”。
- 新增 `analyze_official_docs_structure.py` 和 4 项单元测试；与章节分析器合计 12 项测试。
  检查器记录模块入口、内容类型、前置、步骤、结果、排错、清理、总结、来源和代码角色候选，
  并接入内容质量统一入口；缺失信号仍须人工复核。
- 专项检查发现新的发布阻断：F02 多资产 rolling/drawdown、F02/F03/F06 初始净值基准、
  F06 执行时钟与成本、F08 权限/版本/状态契约、M04 session owner scope、M06 后文直接
  enqueue 双写、M08 高基数 label/空样本/旧 API。整改顺序已写入总控和报告。
- 完成 Wave 0 第一批阻断修复：F02/F03/F05/F06/F07 统一金融数据、回撤、成交时钟、成本、
  YTM 与分类评估契约；F08 整体降为 design-note 并对齐 P03 v0.3 与服务端权限边界。
- M04 session 保存和 trace 改为 owner-scoped；审批新增租户/任务/草稿/动作版本与哈希绑定、
  服务端 approver scope、事务内 CAS、过期/重复决策拒绝和一次性 outbox 恢复。
- M06 从 tasks DDL、队列、幂等、worker 到项目案例统一为 owner-scoped task + outbox 同事务、
  dispatcher claim、CAS、lease、heartbeat 和 reconciliation，删除旧 `/rag/query` 与直接 enqueue。
- M08 metrics middleware 改用路由模板与 `unmatched` 低基数 fallback；空 percentile 不再返回 0；
  Locust 改用环境 bearer、唯一幂等键、`rag_retrieval` 和当前 `/tasks`，并区分 operator JSON
  `/metrics` 与 Prometheus 教学端点。
- 新增 P03 固定时间表 sender：结构化 manifest/payload、Pydantic schema、路径边界、环境
  bearer、`run_id:request_id` 幂等、独立异步计划发送、可选终态轮询和 CSV 证据；3 项测试
  验证慢响应不阻塞后一计划请求，P03 测试由 15 增至 18。
- M09 增补 Secret/Ingress/Job/CronJob/HPA 的真实前置和故障边界；M10 统一 `prompt_tokens`
  并区分 SSE event 与 tokenizer token；M11 增补 Rubric 锚点、双评一致性、固定 trace/window、
  right censoring 和零基线规则；M12 降为 design-note 并重建稳定引用、claim-support、
  检索前权限和 owner-scoped 幂等契约。
- 刷新 202 行结构矩阵：显式类型增至 98 章，包括 72 `instructional`、21 `design-note`、
  1 `reference`、4 `appendix`；未分类由 162 降至 104。自动计数仍只作为人工复核候选。
- Wave 0 最终完整回归通过：七套 reference 共 75 项，311 个 Markdown 的围栏、Python/JSON
  和正式 WikiLink 错误为 0，30 个 Mermaid 文档渲染失败为 0；编码、compileall 和 diff
  检查通过。内容审计为 `passed_with_advisories`：Markdownlint 107、Zhlint 7074、Vale 31
  raw/0 confirmed、Codespell 0、jscpd 3 组，284 个唯一 URL 中 21 个访问异常、0 个确认坏链。
- 逐文件整改矩阵重建为 426 行；已修教材保持 `draft/medium`，不因事实阻断解除而自动授予
  `content-reviewed`。
- 全库显式入口候选为受众 0/22、前置 3/22、版本 2/22、内容类型 5/22；章级为示例
  52/202、来源 38/202、总结 12/202、下一步 5/202。202 章中仍有 162 章未分类；这些数字
  只用于排序，不是缺陷率或教学有效率。
- 新检查器接入后重跑内容审计通过：12 项分析器单元测试、教材与官方格式分析器均返回 0；
  状态为 `passed_with_advisories`。Markdownlint 107、Zhlint 6928、Vale 31 raw/0 confirmed、
  Codespell 0、Git 历史 private-key 0；282 个唯一 URL 中 21 个访问/协议异常，0 个确认 404/410。
- 完整验证入口随后通过：七套 reference 共 72 项测试，311 个 Markdown 的围栏、Python/JSON
  和正式 WikiLink 错误为 0，30 个 Mermaid 文档渲染失败为 0；前后编码门禁、Python 3.13
  compileall 和 `git diff --check` 通过。
- 建立自学教材规范本地映射：结合 Quality Matters、IES/WWC、CAST UDL、Open Textbook
  Review Rubric 和 WCAG 2.2，新增章节质量契约与首批改造报告；明确只报告本地证据，
  不声称外部认证或全库自学就绪。
- F00 第 4-5 章补固定现金流折现、期限敏感性、信用利差边界、point-in-time 反例、
  来源选择 worked example、独立练习和章内来源。
- F01 第 4/6 章补 nearest-rank 与 pandas 插值、VaR/ES 尾部集合、组合方差三项展开、
  资产顺序反例、未见输入练习和可执行围栏。
- F03 第 4-6 章补相关性压力、Sharpe 频率/年化边界、三个权重点和
  “识别-度量-限额-动作-监控-复核”风险治理闭环；明确不构成投资建议。
- M03 embedding 章节补固定关键词向量、cosine 手算、零向量失败和迁移题；实跑发现并修复
  浮点精确断言，改为 ID 精确比较和 `math.isclose(abs_tol=1e-12)`。
- M09 Pod/Deployment 补 observe-break-diagnose-fix-verify、ImagePullBackOff、替代 Pod UID、
  rollout undo 和清理路径；新增命令使用主动 context guard 和显式 `--context=kind-p03-lab`。
- 章节分析器升级为 schema 1.3 并增加 8 项单元测试：支持章级内容类型、冲突诊断、M05
  继承、按类型覆盖率，以及 Worked example/观察实验/独立变式标题；首批结果为 33 个
  `instructional`、5 个 `design-note`、1 个 `reference`、1 个 `appendix`、162 个未分类章。
- 33 个首批 `instructional` 章节的显式证据为目标 33、示例 16、反例 26、练习 24、
  验收 29、章内来源 10；这些数字只用于人工复核排序，不作为教学有效率。
- 6 个显式 `runnable` Python 围栏全部在 Python 3.13.2 环境执行通过；七套 reference
  72 项测试、309 个 Markdown 结构/代码/WikiLink、30 个 Mermaid 文档渲染、compileall、
  `git diff --check` 和前后编码门禁通过。
- 内容审计为 `passed_with_advisories`：Markdownlint 107、Zhlint 6814、Vale 31 raw/0
  confirmed、Codespell 0、Gitleaks Git private-key 0、jscpd 3 组 Markdown；262 个唯一 URL
  中 21 个访问/网络异常、0 个确认 404/410。逐文件整改矩阵重建为 420 行。

## 2026-07-11

- 将 E03 固定语料 BM25 检索接入 P03 v0.3，新增 `rag_retrieval` workload。
- 增加 server-owned reference principal；task payload 不再允许自报 tenant、user
  或 permission groups。
- tasks 表持久化 tenant/user/permission snapshot；幂等键和 task 查询按 owner
  隔离，非 owner 查询统一返回 404。
- RAG worker 在 BM25 排序前同时过滤 tenant 和 permission group，结果持久化
  extractive answer、retrieval timing、授权搜索空间和 source metadata。
- 新增固定 golden queries、公开拒绝、私密授权、跨租户拒绝、空 corpus、
  owner boundary、安全字段注入和零相关候选拒绝测试；P03 共 15 个测试通过。
- 重建 `p03-service:0.3.0` 并通过五服务 Compose 验证；原 outbox、lease、重启、
  重复投递和故障路径保持通过，测试容器与 named volumes 已清理。
- 完成 7 套测试全量回归，共 65 项通过；286 个 Markdown 的围栏、Python/JSON
  代码块和正式 WikiLink 校验通过，全库 Python 3.13 编译及 `git diff --check` 通过。
- E08 增加 500ms queue/outbox/task 时序和独立 worker 容器 CPU/内存采样；
  修复资源采样阻塞队列采样的问题。
- 增加固定随机种子的 1/2/4 worker × 3 次 runner；每组使用空 volume，强制
  Locust 请求数等于数据库 task 数，并导出双侧 95% Student t 区间。
- 本次短时 `mock_rag` reference 共 9 组、HTTP failures 为 0；queue P95 均值
  分别为 2673.41/398.44/225.58 ms。结果保留为本机 reference，不作容量结论。
- Locust workload 增加 `rag_retrieval` 模式；18-task BM25 单轮 smoke 的 HTTP/task
  failures 为 0，全部结果持久化 `retrieval_status=ok` 和非空 sources。
- 安装并校验 kind v0.32.0，固定 Kubernetes v1.34.8 node image 与 digest；新增
  独立 kubeconfig、Kustomize manifests、安装脚本和可清理的端到端验证脚本。
- E09-01/02/03 在 disposable kind 集群完成 PostgreSQL、Redis、API、dispatcher、
  worker rollout；验证 API UID 10001、公开/私密 RAG 权限路径和 API 滚动重启后任务持久性。
- 完成手工 1/2/4 worker replicas 功能对照，每组 24 个任务、0 失败，并分别记录
  1/2/4 个 distinct worker IDs。单次 queue P95 为 5608.94/2055.22/805.13 ms，
  只作为功能证据，不作为 benchmark、容量或科研结论。
- 修复 E09 脚本中文路径解码、Kubernetes 命名用户无法验证 `runAsNonRoot`、readiness
  默认一秒超时和 API rollout 后 port-forward 绑定旧 Pod 四类问题；集群已删除。
- E09-04 Kueue、metrics-server、HPA/KEDA、重复性能实验、持久卷、Ingress/TLS、
  NetworkPolicy 和生产 Secret 继续保持未实现边界；reference 不计为学习者复现。
- 重建逐文件整改矩阵为 380 行；E09-01/02/03 标记为 executable/verified
  reference，E09-04 保持 unverified 概念实验。当时复跑 65 项测试并通过 286 个
  Markdown、全库 Python 3.13 编译和 `git diff --check`。
- 新增 RQ01 合成研究 workload 与 runner：固定 seeds 17-46、三档预测误差、开放环
  Poisson 到达、lognormal task mix 和 FIFO/predicted/oracle common random numbers。
- E2 pilot 完成 90 个条件-seed、270 个策略运行，导出 54,000 条 workload、162,000
  条逐任务结果、seed 汇总、5,000 次配对 bootstrap CI、环境、零失败表和 SHA-256。
- pilot 中 predicted SJF 的 P95 wait 相对 FIFO 改善，但 P99 与 long-class P99 恶化；
  预测误差增大时 P95 收益缩小、尾部伤害扩大。只记录为合成 tradeoff，不外推到 P03/RAG。
- P01 新增 5 个 research runner 测试，七套测试总数增至 70；Pareto/burst、最终 seed
  数、P03 固定到达回放和真实场景 trace 继续保持未完成边界。
- 逐文件整改矩阵随 RQ01 runner、测试、研究模块和 artifact README 重建为 384 行。
- 最终全量验证为 70 项测试、287 个 Markdown、0 个正式 WikiLink 断链、0 个
  围栏/Python/JSON 代码块错误；全库 Python 3.13 编译和 `git diff --check` 通过。
- 引入 Markdownlint CLI2 0.23.0、Zhlint 0.8.2、Vale 3.15.1 和 Lychee 0.24.2；
  Vale/Lychee 二进制按官方 SHA-256 校验，全部以只读模式运行。
- 完成 287 个 Markdown、23 份教材和 153 个唯一外链的内容质量交叉检查；发现
  130 项 Markdown 结构债务、6100 项需校准的中文排版建议和 1 个超大教材文件。
- 修复 FastServe 会议归属及 404 URL、知识卡片模板嵌套围栏、M05 代码标题、M00
  伪标题和交接模板语言标记；重跑后确认 404/410、无语言围栏和反向链接均为 0。
- 新增教材内容质量报告和可复现原始日志；下一阶段优先拆分 M05、统一教材章节契约、
  为 F00-F08/M11 补正文级来源映射，再处理 Markdown/Zhlint 排版债务。
- 修复整改矩阵和仓库验证器对 `.tools` 生成镜像的排除规则，并让验证器支持
  CommonMark 可变长度围栏；最终矩阵为 393 行，正式 Markdown 为 289 个。
- 完成第二层章节级教材审计，将范围校正为 22 份现行教材 + 1 份归档旧稿；新增围栏感知
  分析器，按 202 个现行章节记录目标、示例、反例、练习、验收、来源和视觉证据。
- 新增教材教学有效性与工具复核报告、教材章节质量契约；明确阻断、人工复核和 advisory
  三层门禁，不再用全文件关键词直接判定章节缺失。
- 引入 Codespell 2.4.2、Gitleaks 8.30.1、jscpd 5.0.12 和 Mermaid CLI 11.16.0；
  Codespell 校准后 0 项，Git 历史 private-key 0 命中，24 个含 Mermaid 文档全部渲染通过。
- jscpd 给出 6 组 M05 重复候选，共 94 行、529 token；保留人工复核，不自动删除教学复用。
- M11 新增最小统计推断：实验单位、伪重复、CRN、多 seed 配对差值、效应量、95% CI、
  bootstrap、尾分位数稳定性和多重比较边界，并用 RQ01 artifact 提供可判定复算。
- M08 补闭环/开放环负载、offered/actual/achieved 指标、三测试阶段、coordinated omission、
  固定回放和饱和点；没有把 k6/Vegeta/wrk2 候选写成已执行实验。
- F05 补 Macaulay/modified duration、convexity、有限差分、二阶近似、完整重估和
  clean/dirty price；F04 补风险中性边界、局部 RNG、标准误/区间、多 seed 和解析 oracle。
- F02 补跨资产收益可比性、聚合、时区/DST 和 observation/release/availability/vintage
  时间；F07 补 dataset 清洗、标签信息区间、gap 局限和 validation-only 阈值。
- M01 修复教学字段与 E01/P01 规范字段漂移，补 JSON/dataclass 往返和 pytest 调试闭环；
  M02 对齐 Pydantic v2/E02 契约并补异步与授权边界；M07 容器示例对齐 Python 3.13 非 root 基线。
- 七套 reference 测试再次全量通过，共 70 项；新增教材计算示例分别由固定已知答案、容差
  或原始 artifact 复算验证。
- 逐文件整改矩阵纳入新报告、章节契约和审计工具后重建为 397 行。
- 修正 F00/F01/F03/F06/F08 正文来源映射、F06 回测成交时点与偏差反例、M10
  TTFT/TPOT/throughput/KV cache 口径，并把 Finance reference 增至 9 项测试。
- GF07 固定时间序列扩为 60 个工作日并增加非空切分与两类别断言；RQ01 dirty pilot
  增加源码快照、逐文件哈希和 archive SHA-256，但仍保持 pilot/待 clean rerun 边界。
- 修复章节分析器范围门禁、围栏闭合判断、jscpd 0 文件假扫描和内容审计 blocker 假绿；
  新增七项目 pytest、仓库验证、内容审计、Mermaid、compileall、diff check 统一入口。
- 最终证据更新为七套 reference 71 项测试、291 个 Markdown 结构/代码/WikiLink 错误为 0；
  22 份现行教材共 202 章、1465 个围栏、309 个视觉项。
- 内容审计最终为 Markdownlint 121、Zhlint 6589、Vale 30 raw/0 confirmed、Codespell 0、
  Gitleaks Git private-key 0、jscpd 6 组；246 个唯一 URL 中 20 个网络/访问异常，
  0 个确认 404/410；26 个含 Mermaid 文档真实渲染失败为 0。
- 逐文件整改矩阵最终重建为 398 行；M05 拆分、许可证/第三方来源台账、状态 manifest、
  学习者试学、32 个 Mermaid 读屏元数据和 RQ01 clean rerun 继续保留为未完成项。
- 将 M05 7826 行单文件按固定原始行区间无损拆为兼容索引和 13 个章节；迁移 manifest
  保存原文件及拆分瞬时 SHA-256，515 个围栏和旧标题锚点保持完整。
- E05 全部改为章节直链，新增 E05-05 多 worker 不变量实验、E05-06 证据表达任务和
  M05 reference 导航；高风险成品循环、成本公式和预写结论改为接口、约束与 TODO。
- 修复 P01 文档长期引用但实际缺失的 `examples/run_demo.py`，新增子进程回归测试；P01
  增至 28 项测试，七套 reference 总计 72 项通过。
- 章节分析器升级为 schema 1.2，自研整书分析和 jscpd 均显式聚合 13 个 M05 章节；
  jscpd 从 6 组/94 行/529 token 降至 3 组 Markdown/26 行/285 token，Python 候选清零。
- 本轮完整入口通过，并在迁移说明落盘后复核：308 个 Markdown 结构/代码/WikiLink 错误为 0，Markdownlint 107，
  Zhlint 6601，Vale 31 raw/0 confirmed，246 个唯一 URL 中 0 个确认 404/410，
  30 个 Mermaid 文档渲染失败为 0；矩阵随新增文件重建。
- 全面复核 Obsidian 乱码目录：确认磁盘上仅有一个未跟踪的 `00_` 乱码根目录，内部只有
  两级空目录；经仓库边界和零文件检查后删除，正确的 `00_路线总控` 及其证据未受影响。
- 新增 `tools/validate_encoding.py`，阻断非法 UTF-8、U+FFFD、Unicode 私用区/非字符、
  控制字符、不规范路径后缀、非 NFC 路径和 NFC/大小写碰撞，并输出结构化 JSON。
- 发现 21 个旧日志仅为 PowerShell 5.1 UTF-16 输出，内容无损转为 UTF-8；另一个包含
  712 个 U+FFFD 和 1 个私用区字符的 `repository_validator_post_m05.log` 已从验证器重建。
- 修复统一验证、内容质量审计和 Mermaid 脚本的重定向编码；含中文的 `.ps1` 保持 UTF-8 BOM，
  总入口增加前置与末尾双重编码门禁，防止同一次运行新生成的证据逃逸检查。
- 编码修复后完整入口再次通过：全库编码问题 0，七套 reference 72 项测试通过，308 个
  Markdown 结构/代码/WikiLink 错误为 0，Markdownlint 107、Zhlint 6609、Vale 31 raw/0 confirmed，
  内容审计通过并保留 advisory，30 个 Mermaid 文档 0 失败。
- 逐文件整改矩阵纳入编码门禁和统一验证脚本后重建为 418 行；快速复核使用独立
  `summary_smoke.json`，不再覆盖不跳项完整验证的 `summary.json`。

## 2026-07-10

- 建立整改控制区。
- 纳入教材与内容审计报告。
- 建立状态、验收和分批修订规则。
- 建立全库逐文件整改矩阵骨架。
- 统一 Windows 主学习环境为 Python 3.13，并为可执行参考项目补锁文件。
- 修复 P01 调度状态、ready queue、actual/estimated duration 和 predicted/oracle SJF；22 个测试通过并刷新 CSV/Markdown/SVG artifacts。
- 重构 E02 为单一 FastAPI 项目，3 个端到端测试通过。
- 增加 E03 固定 corpus/黄金集/BM25 参考实现，权限过滤前置，4 个测试通过。
- 增加 P03 v0.2 双模式任务平台：7 个内存测试通过；PostgreSQL、Redis、API、transactional-outbox dispatcher、独立 worker 五服务 Compose 通过正常、失败、积压、租约恢复、API 重启和重复投递验证。
- 为 P03 补任务级 queue wait/total latency，以及 queue/runtime average、P95、P99、outbox backlog 和滚动完成量；空库 Locust smoke 完成 211 次提交、0 HTTP 失败并保留 CSV/JSON 证据，但不作为正式性能结论。
- 将 E07-02/E07-03 从设计稿升级为已验证 reference；E08-01/E08-02 补空库负载与分位数，E08-03 补 run-id 隔离的 1/2/4 worker 单轮对照和 task-processing utilization。
- 增加 E06 SQLite transactional outbox/lease/reconciliation 参考实现，6 个测试通过。
- 修复金融时间序列、未来标签、债券/YTM/久期凸性和 workload 可重放契约；8 个测试通过。
- 补齐 E09-01 至 E09-04，明确 kind 缺失、共享 worker 前置和 Kueue 概念边界。
- 补齐 E10-01 至 E10-03；确定性推理模拟器 7 个测试通过，真实 vLLM 因 Linux/WSL 环境缺失保持 blocked。
- 重写 RQ01 研究问题、prior art、实验 protocol、记录、报告和写作口径；当前仍无有效科研 run。
- 增加全库文档检查器：282 个 Markdown 围栏闭合，Python/JSON 代码块语法通过，正式 WikiLink 断链为 0；保留 100 条概念卡片/模板占位。
- 完成 7 套测试全量复跑，共 57 项通过，并通过全库 Python 3.13 源码编译检查。
