# 修订验证清单

## 环境

- [ ] `python --version` 不再作为唯一解释器检查方式。
- [x] 当前修订项目的主学习命令固定为 `py -3.13` 和项目 `.venv`。
- [ ] 项目声明、教材、实验和 CI/测试使用一致版本范围。
- [x] 当前可执行参考项目均有锁定或可重建依赖清单。
- [x] 含非 ASCII 字符的 PowerShell 5.1 脚本使用 UTF-8 BOM；生成日志统一转为 UTF-8。
- [x] 编码门禁同时检查路径名、严格 UTF-8、替换/私用区字符、控制字符、NFC 和名称冲突。

## 教材与状态

- [x] 主页、任务看板、模块状态表和项目主页使用多维状态口径。
- [x] 设计稿、候选实验、可执行实验和已验证实验明确区分。
- [x] AI 参考答案与本人完成记录明确区分。
- [x] 正式实验/项目入口 WikiLink 已清零；100 条概念卡片/模板占位单独保留，不能当现有资料。
- [x] Markdownlint、Zhlint、Vale 和 Lychee 已完成全库交叉检查；配置固定版本且不自动改正文。
- [x] 311 个唯一 URL 复核后无确认 404/410；25 个 403、超时、FRED/ALFRED HTTP/2
  或缓存异常 URL 单独保留为网络不确定项。
- [x] 教材范围已校正为 22 份现行 + 1 份归档；当前按 210 个现行章节取证。
- [x] Codespell、Gitleaks、jscpd 和 Mermaid CLI 已完成实跑；自动结果按阻断/复核/advisory 分级。
- [x] 教材章节质量契约已覆盖目标、练习、来源、代码角色、图表和成果归属。
- [x] 教材状态已拆为 `content_status`、`instructional_readiness` 与 `learner_validation`；
  Reference 验证另轴记录。本轮官方文档格式检查明确排除试学、本人复现、前后测、迁移和延迟保持。
- [x] Microsoft Learn、Google、Kubernetes、Python 和 GitHub Docs 的 20 个官方页面已核实，
  并按直接规范、官方实现惯例和官方信息架构分层使用。
- [x] 22 份现行教材、210 章已完成确定性矩阵和人工全覆盖复核：金融 63 章、工程 147 章。
- [x] 官方文档结构检查器已输出 210 行 CSV 和 JSON；内容与教学分析器 14 项单元测试通过。
- [x] 章节分析器升级为 schema 1.3；210 章现为 166 `instructional`、2 `workbook`、
  4 `reference`、27 `design-note`、11 `appendix`，`unclassified=0` 且无类型冲突。
- [x] 关键教学章已补章内来源；166 个 `instructional` 章节中显式 worked example 为 78、
  显式章内来源为 51，其余 115 章继续作为人工复核候选。
- [x] 本轮新增或重写的 F01、M00、M01、M02 `runnable` Python 围栏在匹配的
  Python 3.13/reference 环境实跑通过；自动执行前仍需
  哈希允许清单，因为当前执行方式不提供沙箱。
- [x] M05 7826 行章节教材已无损拆为兼容索引和 13 个正文文件；515 个围栏及旧锚点保留。
- [ ] M05 worked example 与学习者任务的答案隔离仍需逐章完成；P01 已确立为唯一 reference 真源。
- [x] F00、F01、F03、F06、F08 五份教材已补正文级来源映射。
- [x] F06 已补可执行回测偏差反例；M10 已补 TTFT/TPOT、throughput、batching
  与 KV cache 可计算模型。
- [x] F02/F03/F06 的 drawdown 基准、F02 多资产 rolling 和 F06 执行时钟/成本已修复并补边界夹具。
- [x] F08 客户端权限自报和过期 P03 契约、M04 session/trace owner scope 与一次性审批、
  M06 不安全直接 enqueue 已统一修复。
- [x] M08 高基数 label、空样本百分位、Locust 认证/幂等和当前 P03 API 契约已修复。
- [x] M09 Secret/Ingress/Job/HPA、M10 token/SSE、M11 Rubric/删失和 M12 引用/权限/幂等/
  claim-support 边界已补齐，未实现能力保持 design-note/blocked。
- [x] M08 固定时间表 sender 已实现 manifest/payload 校验、独立计划发送、环境 bearer、
  owner-scoped 幂等、终态轮询和 CSV 导出；3 项 MockTransport 测试通过。
- [x] 原 104 个 `unclassified` 章节已逐章声明内容类型；类型标签不自动授予内容通过状态。
- [ ] 其余 28 个 Mermaid 仍需补 `accTitle`/`accDescr`，并在发布态 HTML 上试点
  Pa11y/axe 与人工读屏检查。
- [x] 逐文件源文件清单、优化矩阵状态证据、版本 manifest、来源/许可台账、结构化数据和
  RQ01 校验和共六个治理 validator 已接入统一门禁。
- [ ] 来源/许可台账仍有 34 项 `review-required`，根级许可证未选择，M05 及全库练习答案隔离未完成。
- [ ] M01/M03/M09 尚需真实目标学习者试学、迁移题和延迟保持证据。

## 工程正确性

- [x] E02 创建、查询和 metrics 共享同一 app 与 repository。
- [x] E00 进程信号、ready/shutdown 总 deadline 与有界子进程回收通过 11 项测试。
- [x] E01 并发选择、取消和超时边界通过 6 项测试。
- [x] 已修参考实现的状态枚举统一使用 `succeeded` 等显式状态。
- [x] E06 SQLite 参考实现包含 transactional outbox 和 reconciliation。
- [x] E06 缓存键包含租户、数据/索引/模型版本和过滤条件。
- [x] P03 PostgreSQL task/outbox 同事务，Redis 只传递 `task_id`。
- [x] P03 dispatcher claim、worker CAS、重复投递和过期租约恢复经过真实 Compose 验证。
- [x] P03 v0.3.1 bearer principal、owner-scoped 幂等/查询、tenant + permission
  prefilter、零相关候选拒绝、持久化 source metadata、Redis Streams 恢复和 fixed replay sender
  通过 27 个测试；
  原 Compose 故障路径保持验证证据。
- [x] E07-02/E07-03 的五服务启动、依赖 readiness 和 API/worker 故障路径有自动脚本。
- [x] E08-01/E08-02 有空库 Locust CSV、task 分位数和局限说明。
- [x] E08-03 已有随机化 1/2/4 worker × 3 次本机对照、队列时序、
  task-processing utilization、worker 容器 CPU/内存和 95% t 区间。
- [x] M08 已区分闭环并发和开放环到达率，并补 offered load、实际发起率、测试阶段、
  coordinated omission、固定回放和饱和点判定。
- [ ] E08 学习者正式实验仍需长时稳态、warm-up 规则、重复 RAG/LLM、
  raw task export 和更强主机隔离。
- [x] E09-01/02/03 已在 kind v0.32.0 + Kubernetes v1.34.8 完成多服务 rollout、
  非 root UID、RAG 权限路径、API 滚动重启持久性和 1/2/4 worker 功能性扩缩容验证。
- [ ] E09 尚未覆盖 metrics-server、HPA/KEDA、重复性能实验、持久卷、Ingress/TLS、
  NetworkPolicy 和生产 Secret；单次时延只作功能证据。

## 安全

- [x] RAG 权限在检索数据层前置过滤并有泄漏测试。
- [x] E03 多格式解析、质量报告、tombstone-first lineage 删除和 simulated generation 输出评估
  已纳入严格测试；artifact inventory 也要求服务端 Principal、query scope、tenant 与 ACL，未授权时
  不泄露文档是否存在。
- [x] RAG task payload 不能自报 tenant/user/permission；非 owner 查询返回 404。
- [x] Agent session 读取、保存和 trace 强制使用 user/tenant 所有权范围。
- [x] Agent 审批绑定租户、任务、草稿/动作版本与哈希，并用服务端授权、事务和 CAS
  拒绝越权、过期与重复决定。
- [x] E04 reference 使用严格 tool schema、capability/resource 授权、versioned reducer/checkpoint 和运维审计脱敏，并有 86 项正负向测试。
- [ ] 全库仍需补齐 egress/path/URL 验证、外部工具副作用和生产日志敏感信息审计。

## 科研

- [x] oracle SJF 与 predicted SJF 分开报告。
- [x] 30 seeds、三档预测误差、开放环 Poisson 到达和 lognormal task mix 已纳入 E2 pilot。
- [ ] Pareto 重尾、on/off burst、worker-count sensitivity 和最终 seed 数仍待完成。
- [x] nearest-rank run 内百分位、seed 级配对差异、效应大小和 95% percentile bootstrap CI 已输出。
- [x] RQ01 runner、54,000 条原始 workload、162,000 条逐任务结果、环境、源码快照
  和 SHA-256 清单已验证；dirty pilot 保持为 pilot，不冒充 final run。
- [ ] RQ01 尚需从 `source_snapshot.tar.gz` 解包到 clean 环境后重跑，才能升级为 final research run。
- [x] M11 已补实验单位、伪重复、多 seed 配对差值、效应量、t/percentile bootstrap
  区间和多重比较边界，并可复算 RQ01 artifact。
- [ ] P03 固定时间表 sender 已实现，但真实 P03 服务回放与真实 RAG/Agent trace 尚未完成。

## 金融

- [x] 唯一键为 `(symbol, date)`，收益率按资产分组计算。
- [x] 未来标签先移除空值，再生成分类标签并设置时间 gap。
- [x] 债券现金流支持付息频率，YTM 根区间经过验证。
- [x] Macaulay duration、modified duration 和 convexity 有已知答案测试。
- [x] workload 保存不可变输入、hash、版本、随机种子和 artifact URI。
- [x] F05 教材已补久期/凸性公式、有限差分、二阶近似、完整重估和 clean/dirty price 边界。
- [x] F04 教材已补风险中性语义、局部 RNG、标准误/区间、多 seed 和解析 oracle。
- [x] F02 教材已补跨资产收益口径、时区/DST 和 point-in-time 五类时间；F07 已补
  dataset 清洗、标签信息区间、gap 边界和 validation-only 阈值。

## 仓库与发布安全

- [x] Git 历史 Gitleaks private-key 规则 0 命中；完整规则误报已人工复核。
- [x] 私有 Rubric/案例来源已登记为 `EXT-RUBRIC-001`/`EXT-CASE-001`；原件、内部标识和本机路径
  已从公开候选移除，仅保留脱敏作者摘要。
- [ ] 若公开分发整个工作目录，必须排除被 Git 忽略的 Obsidian 配置、`.tools` 和 `.venv`，
  并在工作区曾外发时轮换本地证书/凭据。

## 本轮统一验证

- [x] 截图中的乱码空目录已在确认位于仓库内且不含文件后删除；唯一损坏日志已重建。
- [x] 编码门禁在统一入口的前后各运行一次，修复后全库路径与文本问题均为 0。
- [x] 十套 reference 共 386 项测试通过：E00 11、E01 6、E02 29、E03 154、E04 86、
  E06 29、E10 7、P01 28、Finance 9、P03 27；这些只证明 reference，不代表学习者完成或生产可用。
- [x] 仓库验证器的围栏、Python/JSON 和正式 WikiLink 错误均为 0；100 条概念卡片/模板
  占位 WikiLink 单独报告，不冒充现有资料。
- [x] 完整内容审计为 `passed_with_advisories` 且 blocker 为 0；格式、措辞、重复候选和网络
  不确定项仍按 advisory/人工复核解释，不折算为教学质量分数。
- [x] Mermaid CLI 对 30 个含 Mermaid 的文档完成真实渲染，失败为 0。
